import React from "react";

import "../css/ProductItem.css";

const apple = "/media";

const ProductItem = ({ title, children }) => {
	return (
		<div className="productItem">
			<img src="" alt="" />
		</div>
	);
};

export default ProductItem;
